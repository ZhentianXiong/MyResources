基础数据结构：优点-缺点
数组：通过索引可以直接访问任意元素-----初始化时就要知道元素的数量
链表：使用空间大小与元素数量成正比-----需要通过引用访问任意元素

系统标准库：
System Integer Double String StringBuilder Math

原始数据类型：
boolean char int double

全部为值传递：原始数据类型直接传递值，引用类型传递的是引用的值即引用地址

数组：可变，传到方法中会改变
String：不可变，传到方法中也不变

数量级：
常数：1 普通语句
对数：log N 二分策略，二分查找
线性：N 循环，找出最大元素
线性对数：N log N 分治，归并排序
平方：N^2 双层循环
立方：N^3 三层循环
指数：2^N 穷举查找

类型内存子节大小：
boolean 1 (计算机访问内存是一次一字节)
byte 1
char 2
int 4 (32位，共2^32个值，-2^31-2^31)
float 4
long 8
double 8
一个对象一般都会被保存为8字节的倍数，因为64位计算机中的机器字为8字节（填充，而且对象一定会有16字节的对象开销如垃圾收集等）
对象的引用一般都是一个内存地址，8字节
数组需要额外4字节保存长度，所以头部一般24字节（16对象开销+4长度+4填充）+N其他存储
String对象本身 40字节 = 16对象开销 + 8对象引用 + 4偏移量（某一段的起点） + 4长度（从起点往后的长度） + 4散列值 + 4填充
String对象存为char数组，共 40 +24+2N，但是之后substring（）只需要40字节，因为40字节中有记录截取char数组的哪一段
当程序调用一个方法时，系统会从内存中的栈中分配所需的内存来保存局部变量
通过new创建对象时，系统会从堆中为对象分配所需的内存，对象会一直存在，直到其引用消失为止

